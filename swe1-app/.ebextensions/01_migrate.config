container_commands:
  00_prepare_sqlite:
    command: |
      touch db.sqlite3
      chown webapp:webapp db.sqlite3
      chmod 664 db.sqlite3

  01_migrate:
    command: "su -s /bin/bash -c '. /var/app/venv/*/bin/activate && python manage.py migrate --noinput' webapp"
    leader_only: true
